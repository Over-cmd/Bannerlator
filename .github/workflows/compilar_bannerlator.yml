name: Build Bannerlator Optimized for Android 11

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 1. Clonar código fuente de Bannerlator
      uses: actions/checkout@v4

    - name: ⚙️ 2. Configurar Entorno de Java JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: 🛠️ 3. PARCHE DE ARQUITECTURA: Forzar SDK de Android 11 (API 30)
      run: |
        # Modificamos el targetSdk en el archivo build.gradle principal para eliminar 
        # los bloqueos gráficos y los desbordamientos de memoria en Android 11
        find . -name "build.gradle" -exec sed -i 's/targetSdk 28/targetSdk 30/g' {} \;
        find . -name "build.gradle" -exec sed -i 's/targetSdkVersion 28/targetSdkVersion 30/g' {} \;

    - name: 🤖 4. Configurar Android SDK y NDK
      uses: android-actions/setup-android@v3

    - name: 🏗️ 5. Compilar APK Oficial via Gradle en la Nube
      run: |
        chmod +x ./gradlew
        ./gradlew assembleRelease --no-daemon

    - name: 📤 6. Subir APK Optimizado a los Artefactos
      uses: actions/upload-artifact@v4
      with:
        name: Bannerlator-Bifrost-Android11-APK
        path: app/build/outputs/apk/release/*.apk
