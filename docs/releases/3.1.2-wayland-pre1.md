<p align="center">
<img src="https://github.com/The412Banner/Bannerlator/blob/main/logo.jpg?raw=true" width="1500" height="500" alt="Bannerlator" />
</p>

<p align="center">
<img src="https://img.shields.io/badge/release-3.1.2--wayland--pre1-ff3d8b?style=for-the-badge" alt="3.1.2-wayland-pre1" />
&nbsp;<img src="https://img.shields.io/badge/pre--release-testers%20only-f59e0b?style=for-the-badge" alt="pre-release" />
&nbsp;<img src="https://img.shields.io/badge/update-not%20offered%20in--app-64748b?style=for-the-badge" alt="not offered in-app" />
</p>

# Bannerlator 3.1.2 — Wayland pre-release 1

Run Windows apps and games on Android — no PC and no root required.

**This pre-release adds a second display backend: Wayland.** Instead of the Java X server, a game can draw through an embedded Wayland compositor that Bannerlator runs itself. It is a tester build: everything here has been proven on one device (an AYANEO Pocket FIT, Adreno 750) and nothing else. It is not offered by the in-app updater and it does not replace 3.1.1 — you install it over 3.1.1 by hand, and your X11 containers keep working exactly as before.

<!-- update-summary: Wayland pre-release for testers: a second display backend with pointer lock, keyboard, clipboard and per-game driver choice. Not offered in-app. -->

> ⚠️ **Read before testing.** Wayland is opt-in per container. It needs the Wayland Proton layer attached to this release (`proton-11.0-2.1-arm64ec-wayland-v3.wcp`); on any other layer the Wayland choice is greyed out and a launch falls back to X11 with a notice. Expect rough edges listed under *What it cannot do yet*.

# What is in this release (all three flavours)

| File | What it is |
|---|---|
| `Bannerlator-3.1.2-wayland-pre1-standard.apk` | the normal app (`com.winlator.banner`) |
| `Bannerlator-3.1.2-wayland-pre1-pubg.apk` | the "pubg" package name flavour (`com.tencent.ig`) |
| `Bannerlator-3.1.2-wayland-pre1-ludashi.apk` | the Ludashi package name flavour |
| `proton-11.0-2.1-arm64ec-wayland-v3.wcp` | the Wayland Proton layer (Proton 11.0-2 + winewayland + four bundled Turnips), installs as **Proton-11.0-2.1-arm64ec-3** |
| `README-Wayland-test-kit.txt` | the same setup notes as below, for sharing |

# Setup

1. Install the APK for your flavour over your current install (data is kept).
2. Install the wcp: **Contents → Proton → Install from file**. It installs as `Proton-11.0-2.1-arm64ec-3`, its own layer line, so it is never offered as an "update" to a normal 11.0-2 container. Remove the older `-1` / `-2` entries if you had them from the test kit.
3. Make sure an **Android Turnip** driver is installed (any recent one from the in-app catalog is fine). On Wayland that driver is used by the compositor to put frames on screen. Do **not** pick *System* as the compositor driver — the stock driver cannot import the game's frames and you get a black screen.
4. Container settings: Proton = `Proton-11.0-2.1-arm64ec-3`, **Display backend = Wayland**, Compositor driver = your Android Turnip, Wayland game driver = **Auto**, FEXCore = an installed version. DXVK / VKD3D, Wine components, esync and audio: the same as you use on X11.
5. The **first launch after installing the wcp comes up at the wrong size** (1024x768). Just relaunch.

# What's New — everything since 3.1.1

## 🌊 Wayland display backend

- **Per container, with a per-game override** (Use container default / Force X11 / Force Wayland) in the container editor, the game shortcut editor and the XMB view.
- **Gated on a capable layer.** Wayland is only offered when the selected Proton ships winewayland and its Wayland Turnip; anything else falls back to X11 with a notice, so a wrong setting can never hide a game.
- **The settings tell the truth on Wayland.** Renderer reads "Vulkan (Wayland compositor)"; Render scale and Frame Generation are greyed with the reason; Fullscreen Mode, Screen Alignment, FPS limiter, DX wrapper, audio, emulator and refresh rate all work.
- **Every graphics API switches fluidly in one session:** Vulkan, OpenGL (through Zink), D3D12, D3D11, D3D10, D3D9, D3D8 and DirectDraw, proven with the AIO Graphics Test switching between all of them in one launch without a crash.

## 🎯 Pointer lock and mouse-look

- The compositor implements pointer constraints and relative motion, the two Wayland protocols Wine uses for games that grab the mouse. Touch drags, a captured mouse and stick-as-mouse all reach the game as relative motion.
- **Proven:** Half-Life 2 at 144 fps with touch mouse-look, including a full turn-around.
- The in-game *Relative Mouse* chip and *Mouse Warp Override* are live on Wayland.

## ⌨️ Keyboard, clipboard and the soft keyboard

- **Keyboard input works on Wayland** — it never did in the earlier test kits. Two causes were fixed: winewayland gave up its keyboard when it could not read xkb registry data, and the compositor handed keys to Wine's desktop instead of the window you clicked.
- **Clipboard both ways:** copy in a Windows program and paste on Android, copy on Android and paste in the program.
- **The soft keyboard opens by itself** when a Windows text field takes focus (tap into the field first) and closes when it loses it.

## 🖥️ Fullscreen modes, alignment and the compositor

- Fullscreen Mode (Off / Fit / Stretch / Fill / Integer) and Screen Alignment (Center / Top / Bottom) now apply on Wayland, live from the in-game drawer too, with touch input mapped to match.
- Compositor fixes: no black frame or window churn when a game rebuilds its swapchain, HOME and back brings the picture back, the FPS cap stays exact, HUD sampling no longer stalls the compositor thread, and the swapchain recovers when the screen surface is lost.

## 🧩 Per-game Wayland driver choice — four Turnips built into the Proton

On Wayland the game renders on a Vulkan driver bundled inside the Proton layer, chosen under **Wayland game driver** (Auto picks by your GPU):

| Choice | Built from | Adreno target |
|---|---|---|
| Bundled | upstream Mesa `7cda7850`, no device patches, [Banners-Turnip `wayland` branch](https://github.com/The412Banner/Banners-Turnip/tree/wayland) | 6xx, 730, 740, 750 |
| Bundled a7xx | [Vauzi-17 "710" v3.6](https://github.com/Vauzi-17/710/releases/tag/3.6) recipe (`add_710_720_722.py`), Mesa `7631b525` | 710, 720, 722 |
| Bundled a8xx | [WinNative WN-Turnip 1.15](https://github.com/WinNative-Emu/Drivers/releases/tag/v1.15) **Balanced** recipe, Mesa `12b7b819` — Auto's pick on an 8xx | 830, 840 (+ 810 / 825 / 829 entries) |
| Bundled a8xx Performance | [WinNative WN-Turnip 1.15](https://github.com/WinNative-Emu/Drivers/releases/tag/v1.15) **Performance** recipe (holds the GPU at PWR_MAX), Mesa `12b7b819` | same |

All four are rebuilt from those sources as Wayland/Linux drivers (KGSL backend, Wayland WSI) — the Android zips those projects publish cannot be used inside Proton. You can also import your own Wayland-built driver as a zip under **Contents → Wayland game drivers (Linux ICD)**. Vauzi recommends `TU_DEBUG=sysmem` on 710/720/722 if you see artifacts (set it in the container's Env Vars).

## 🧪 Experimental: the game on its own Android layer

Put `BANNER_WAYLAND_ZERO_COPY=1` in a container's or shortcut's Env Vars and a fullscreen game is presented on its own Android hardware layer, composed by the display hardware instead of the app. Proven with Half-Life 2 (hardware composition at 144 Hz). It does not save a copy yet — it is the first half of the zero-copy work below. Leave it unset for normal play.

## 🔧 Smaller

- A shortcut whose executable has no folder or no `.exe` no longer crashes the launch.
- The wcp's Zink / EGL libraries now come from the same build as its Turnips.

# What it can and cannot do right now

**Can:** run DXVK / VKD3D / Zink games on Wayland with the same drivers and settings you use on X11; switch graphics APIs freely; mouse-look; keyboard; clipboard; soft keyboard; fullscreen modes; the FPS cap; background and resume; pick a bundled driver per game.

**Cannot yet, or not proven:**
- **It is not faster than X11 today.** On the AIO test Vulkan and D3D12 run about 20% behind X11 (596 vs 752 fps, 336 vs 430), the DXVK paths are within a few percent, and OpenGL is ahead (230 vs 172). The compositor still copies every frame once; in a real GPU-bound game the difference is a few percent either way. Power draw has not been measured against X11.
- **Only one device has run it.** The a7xx and a8xx drivers are proven to load and render on an Adreno 750, not on real 710/720/722 or 830/840 hardware. Reports from those GPUs are the most valuable thing a tester can send.
- Keyboard layouts default to US (the xkb registry data is not bundled yet).
- Drag-and-drop and image clipboard are not bridged (text only); typed-text deletion from the soft keyboard is sent as key presses.
- Frame generation (LSFG / Win-FG) is not wired to the Wayland path and stays greyed out.
- Launching Half-Life 2 the way the test kit does closes Wine's desktop process a second in; fullscreen games do not care, windowed games launched that way lose the taskbar.
- Window icons and title-bar decorations are missing.
- Leaving the app pauses the whole container; come back and it resumes. That is by design.

# What is left — fixes and performance

1. **Performance:** advertise the Qualcomm compressed buffer format so game swapchains are no longer forced linear (cheap, to be A/B tested first), then the real zero-copy path — a swapchain change in our own Wayland Turnip so its images are Android buffers the layer path presents directly. The Android half is already in this release behind the flag above.
2. First launch after a wcp install at 1024x768.
3. The desktop-process close on that Half-Life 2 launch.
4. Bundle xkeyboard-config so non-US layouts get their names.
5. Real 710/720/722 and 8xx testing of the bundled drivers.
6. Frame generation on Wayland.
7. Later: presentation feedback for even frame pacing, tearing control, native multi-touch, HDR / colour management, window decorations.

# Logs, if something goes wrong

- `Download/Wayland-logs/wayland-<date>_<time>.log` — the compositor's session log: windows, frames on screen per 10 s, pointer lock, clipboard, text input, screen mode.
- `Download/bannerlator/<game name>/wine_debug.log` and the DXVK / VKD3D logs next to it (turn on Wine debug logging in Settings → Log Manager).

## ⚠️ Carried over — still open

- Everything listed as open in 3.1.1 is unchanged here: Win-FG Native's limited device testing, the untested Steam Controller puck, cancelling a store download, Origin-era EA sign-ins, Need for Speed (2015), Rockstar Games Launcher titles, 64-bit titles launched directly by Steam, and the Java Steam engine fallback.

<details>
<summary><b>📦 Where to get Proton 9</b> (tap to expand)</summary>

The APK does not bundle Proton 9 — Wine installs from the in-app catalog on first run. The Wayland layer attached to this release is Proton 11.0-2 and is **not** in the catalog: install it from the file above.

**Proton 9.0 arm64ec** — not bundled, download it if you want it:

**https://github.com/The412Banner/Nightlies/releases/download/Proton/wine/proton-9.0-arm64ec.wcp**

66.7 MB · sha256 `f8a99fed387f1b097009129dc49368c8f500927640a9d4c7a192d850b1b2068c`
Install it via **Containers → download icon → install the `.wcp`**. It also appears in the in-app catalog. *(Compiled as a wcp by **Xnick417x**.)*

</details>

---

<details>
<summary><b>🙏 Credits</b> (tap to expand)</summary>

**The bundled Wayland drivers are rebuilt from community work:** the 710/720/722 recipe by **[Vauzi-17](https://github.com/Vauzi-17/710)** (release 3.6) and the Adreno 8xx Balanced and Performance recipes by the **[WinNative development team](https://github.com/WinNative-Emu/Drivers)** (WN-Turnip 1.15), both on top of **Mesa / Turnip** by the Freedreno and Mesa teams. The plain driver, the Wayland WSI builds and the Proton integration are from **The412Banner**'s [Banners-Turnip](https://github.com/The412Banner/Banners-Turnip) and [proton-wine](https://github.com/The412Banner/proton-wine) forks.

The embedded Wayland compositor, its pointer-lock, clipboard and text-input support, and the Android-layer prototype are original work by **The412Banner**.

Built on the work of many open-source projects and their communities:
- **Display** — **Wayland** (the freedesktop.org community) and **Wine**'s `winewayland.drv` (WineHQ).
- **Graphics** — **DXVK** (Philip Rebohle & contributors), **VKD3D-Proton** (Hans-Kristian Arntzen & contributors), **Mesa / Turnip / Zink** (the Freedreno & Mesa teams).
- **Containers, launch & runtime** — **Wine** (WineHQ) and **Proton / GE-Proton** (Valve & GloriousEggroll); **box64** (ptitSeb) and **FEXCore** (the FEX-Emu team).
- **Native Steam engine & SteamLite agent** — derived from **WinNative**'s GPL `wnsteam` client and `wn-steam-launcher`; protocol groundwork from **SteamKit2**.
- **Audio** — Android **AAudio**, driven by our own **DirectAudio** endpoint.

</details>
